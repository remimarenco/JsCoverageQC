/*function upload_and_checkFireDone(done, object_XMLHttpRequest_And_ValueToStore_forUpload){
	// Check first if done has been emitted
	var allReady = true;
	object_XMLHttpRequest_And_ValueToStore_forUpload.othersXMLHttpRequest.forEach(function(otherXMLHttpRequest){
		if()
	});
	if(reqTsv_readyState === 4 && (reqTsv_status === 200 || reqTsv_status === 0)  && !done_emitted){
		done();
	}
}


var reqTsv = new XMLHttpRequest();
reqTsv.open("GET", tsvFile, true);
reqTsv.responseType = "text";
reqTsv.jsCoverageQC.valueToStore
upload_and_checkFireDone(done, {
	XMLHttpRequest,
	valueToStore,
	othersXMLHttpRequest,
	done_emitted
});*/